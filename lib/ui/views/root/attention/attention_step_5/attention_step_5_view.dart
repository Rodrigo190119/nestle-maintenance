import 'package:flutter/material.dart';

class AttentionStep5View extends StatelessWidget {
  const AttentionStep5View({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Container());
  }
}
