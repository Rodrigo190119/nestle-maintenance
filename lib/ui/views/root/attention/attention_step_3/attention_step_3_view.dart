import 'package:flutter/material.dart';

class AttentionStep3View extends StatelessWidget {
  const AttentionStep3View({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Container());
  }
}
