import 'package:flutter/material.dart';

class AttentionStep2View extends StatelessWidget {
  const AttentionStep2View({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Container());
  }
}
