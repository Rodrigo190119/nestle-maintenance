import 'package:flutter/material.dart';

class AttentionStep4View extends StatelessWidget {
  const AttentionStep4View({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Container());
  }
}
